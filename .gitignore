# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Sensitive variable files — never commit real credentials
terraform.tfvars
*.auto.tfvars

# Packer
packer_cache/
*.pkr.hcl.lock

# SSH / secrets
*.pem
*.key
id_rsa
id_ed25519

# Ansible generated inventory (contains dynamic IPs, regenerated by terraform apply)
ansible/inventory/hosts.ini

# OS
.DS_Store
Thumbs.db
